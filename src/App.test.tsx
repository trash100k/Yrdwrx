import { describe, it, expect } from 'vitest';
import { render, screen } from '@testing-library/react';
import React from 'react';

describe('Placeholder tests to get vitest working', () => {
    it('Should return true', () => {
        expect(1).toBe(1);
    })
})
